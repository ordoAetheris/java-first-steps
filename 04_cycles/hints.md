# Подсказки: Циклы
## 1: for (int i = 10; i > 0; i--) — обратный цикл
## 2: for (int i = 1; i <= 10; i++) println(n + " x " + i + " = " + n*i)
## 3: while (n > 0) { sum += n % 10; n /= 10; }
## 4: Random random = new Random(); int target = random.nextInt(100) + 1; Scanner scanner = new Scanner(System.in);
## 5: Вложенные циклы: for каждого числа → проверить делители от 2 до sqrt(n)
## 6: a=0, b=1, следующее = a+b, потом сдвиг: a=b, b=next
## 7: i-- уменьшает i → условие i<10 ВСЕГДА true → бесконечный цикл. Ctrl+C для остановки
## 8: Первая и последняя строка — все *. Средние — * пробелы *
## 9: Внешний цикл по строкам, внутренний по числам
## 10: Random.nextInt(menu.length) для случайного индекса
